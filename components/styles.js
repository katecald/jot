import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 25,
        padding: 10,
    },
    welcome: {
        fontSize: 20
    },
    instructions: {
        fontSize: 10
    }
})

export default styles